export default {
    mounted() {
        window.addEventListener("load", () => {
            window.scrollTo(0, 0);
        });
    },
};
/* Permet le retour en haut de page quand on fait un retour arrière d'une gallerie à revenir au portfolio */
